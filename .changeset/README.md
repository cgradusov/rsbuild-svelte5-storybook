# Changesets

This directory holds Changesets — small markdown files describing what changed
and how the version should bump. Run `bun x changeset` to create one.

See [the docs](https://github.com/changesets/changesets) for details.
